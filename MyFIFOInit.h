/** @file MyFIFOInit.h
 * @brief The function MyFIFOInit() inizialize a vector
 *
 * This function create a vector with size that 
 * is pass in input. The value of the each element
 * in the vector is inizialized to zero. The output
 * of the function is the vector.
 * 
 * @author Bego Giacomo
 * @date 22 March 2022
 * @bug No known bugs.
 */

#ifndef _MYFIFOINIT_H
#define _MYFIFOINIT_H

/**
 * @brief The function MyFIFOInit() inizialize a vector.
 *
 * This function create a vector with size that 
 * is pass in input. The value of the each element
 * in the vector is inizialized to zero. The output
 * of the function is the vector.
 * @verbatim 
 * This is my email:giacomo.bego@ua.pt
 * @endverbatim
 * Example of usage:
 * @code
 * vect = MyFIFOInit(VectSize);
 * @param  size It's the size of the new vector.
 * @return Return a vector with each lement null 
 */

void MyFIFOInit(int size);
#endif
